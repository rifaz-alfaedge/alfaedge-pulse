// feather-icons ships no types of its own, and there's no @types package
// for it — see MeterIcon.tsx for the (small, static-icon-only) surface
// this app actually uses.
declare module 'feather-icons'
